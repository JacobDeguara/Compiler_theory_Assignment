fun MoreThan50 ( x : int )  bool {
let x : int = 23 ; // syntax ok , but t h is should not be allowed ! !

if ( x <= 50) {
    return false ;
}
    return true ;
}

let x : int  = 45 ; // t h is is f i n e
while ( x < 50) {
    __print MoreThan50( x ); //”false ” x6 since bool operatoris <
    x = x + 1 ;
}

let x : int  = 45 ; // re−declaration in the same scope . . . not allowed ! !
while (MoreThan50 ( x )) {
    __print MoreThan50 ( x ); //” false” x5 since bool operatoris <=
    x = x + 1 ;
}

let w: int  = width ;
let h : int  = height ;

for ( let u : int  = 0 ; u<w; u = u+1)
{
    for ( let v : int  = 0 ; v<h ; v = v+1)
    {
        pixelr u , v ,1 ,1 ,#00ff00 ;
    }
}