fun MoreThan50 ( x : int ) -> bool {
    let x : int = 23 ;  // re-declaration in same scope !ERROR!

    if ( x <= y) {
        return false ;
    }
    return true ;
}

let x : int  = 45 ;

while ( x < 50) {
    __print MoreThan50 ( x );  // this prints 'false' 6 times since bool operator is <
    x = x + 1 ;
}

let x : int  = 45 ;  // re-declaration in same scope !ERROR!
while (MoreThan50 ( x )) {
    __print MoreThan50 ( x ); 
    x = x + 1 ;
}

let w: int  = __width ;
let h : int  = __height ;

for ( let u : int  = 0 ; u<w; u = u+1)
{
    for ( let v : int  = 0 ; v<h ; v = v+1)
    {
        __pixelr u , v ,1 ,1 ,#00ff00 ;
    }
}